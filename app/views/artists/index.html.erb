<h1>Artists</h1>

<% @artists.each do |artist| %>
    <ul>
        <li><%= link_to artist.name, artist_path(artist) %>
           <%= link_to 'Update Artist', edit_artist_path(artist) %></li>
    </ul>
<%end%>
<br>
<%= link_to "Create Artist", new_artist_path %> 