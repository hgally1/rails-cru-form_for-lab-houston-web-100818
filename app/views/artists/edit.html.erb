<h1>My edit <%= @artist.id %> </h1>
<%= form_for Artist.new do |form| %>
    <%= form.label :name %>
    <%= form.text_field :name %>
    <%= form.label :bio %>
    <%= form.text_field :bio %>

    <%= form.submit "Update Artist"%>
    <%end%> 